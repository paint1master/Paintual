<UserControl x:Class="PaintualUI.Controls.ColorPicker.ColorPlaneSelectionGlass"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PaintualUI.Controls.ColorPicker"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!-- Background transparent is required for mouse events to work
    otherwise grid doesn't seem to exist even if size is set
    to use all available width and height, mouse event don't work. -->
    <Grid x:Name="Glass" Background="Transparent" MouseDown="Glass_MouseDown" MouseUp="Glass_MouseUp" MouseMove="Glass_MouseMove">
            
    </Grid>
</UserControl>
